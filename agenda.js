function novoLembrol() {
    const novoLembrol = document.getElementById('novoLembrol');
    novoLembrol.classList.remove('d-none');
}

function cancelar() {
    const cancelar = document.getElementById('novoLembrol');
    cancelar.classList.add('d-none')
}